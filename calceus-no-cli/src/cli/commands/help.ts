export const help = () => {
    console.log("calceus cli tool")
    console.log("run [calceus] to build new project from scratch")
    console.log("run [calceus] to build run environment actions inside the project with .calceus file")
}